interface Window {
    /**
     * Indicates whether or not the current environment is in debug mode.
     * Should correspond to using debug vs. minified scripts.
     */
    $DEBUG: boolean;
}
/**
 * Indicates whether or not the current environment is in debug mode.
 * Should correspond to using debug vs. minified scripts.
 */
declare const $DEBUG: boolean;
